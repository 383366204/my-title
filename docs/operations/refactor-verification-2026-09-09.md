# 通用工作流重构验收

日期：2026-09-09。范围：通用 Web 状态/连接、合规选品/铺货接口、共享 HTTP 基础设施与样式组织。刷单、虚假评价专用功能未继续开发或迁移，不在本次完成声明内。

## 检查结果

| 检查 | 结果 |
| --- | --- |
| npm test | 210 通过，1 项真实平台 smoke 跳过 |
| core/test | 116 通过 |
| 1688 与标题生成测试 | 82 通过，执行时拦截外部网络与 9222 连接 |
| pipeline-flow | 94 通过，使用离线替身 |
| 路由契约 | 原 70 组路径、方法、中间件数量保持兼容 |
| 浏览器命令/确认竞态 | 19 个 React StrictMode 场景通过 |
| 浏览器历史/SSE/目录 | 乱序成功/失败、切换模板、删除、卸载、回放去重、终态和列表刷新通过 |
| 浏览器视口 | 1440×1000、980×800、390×844；五种选品/分析模板、侧栏、历史、长产物及手动输入滚动通过 |
| 生产构建 | 通过；原有单包 >500 kB 提示保留 |
| 新后端模块 | Node 语法、no-undef 检查通过 |
| 补丁空白检查 | git diff --check 通过 |

主测试与共享层/专项存在部分重叠，以上数字不能简单相加当成不重复测试数。没有执行包含专用表格测试的完整 test:all，也没有用真实商家数据提交来验收。

## 已核实变化

- WorkflowStudio 由约 780 行降为 519 行；会话、确认、运行事件和规则各自有明确入口。
- workflow-ui 保留 72 行兼容入口；pipeline-adapter 保留 30 行兼容入口，原公共签名保持不变。
- server 由约 2463 行降为 708 行，通用路由及任务/状态服务已迁出。剩余专用业务代码没有为了减少行数而继续拆分。
- 原 5832 行 App.css 按连续区段拆分，首次拆分的构建 CSS 与基线逐字节相同。窄屏修复单独置于最后的 responsive-workspace.css，未混入纯迁移。
- 新增历史刷新代次保护、删除后旧响应失效、局部刷新错误处理；此前批次的命令隔离、SSE 恢复和铺货准备阶段防重也纳入回归。

## 保留边界

- 测试使用 mock API 和临时文件，不证明当前 Chrome 登录态、淘宝/生意参谋权限或真实铺货可用。
- 互斥仍是服务实例内机制；不是跨进程锁。未重写评分、限流、平台采集器和深层业务算法。
- 单包仍约 639 kB；快速切换布局时偶发 ResizeObserver 通知，测试中未观察到画布消失或操作失败。没有添加全局忽略错误的代码。
- CSS 连续区段之间仍存在原有级联依赖，拆分不等于全部样式去重。维护规则见 styles/README.md。
- Antigravity 参与了方案评议；最终一轮是无工具评议，不应称为独立仓库审计。

## 当前运行状态

生产前端已构建，原 3000 服务首页返回 200。另启动前端开发预览 http://127.0.0.1:5174/，API 代理到 3000。没有重启原后端，后端文件改动需在合适时机重启后生效。

没有提交、合并或推送。本次没有编辑既有 data/keyword-mining/seeds.json 数据变更。

截图位于本机临时目录 `/tmp/workflow-studio-final/`，包含 initial、diagnostics、artifact 和 manual-input 三个视口的图片。长期维护入口见 workflow-code-map.md。
